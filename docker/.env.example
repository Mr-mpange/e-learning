# Frappe LMS Docker Environment Configuration
# Copy this file to .env and modify as needed

# Database Configuration
MYSQL_ROOT_PASSWORD=123
DB_HOST=mariadb
DB_PORT=3306

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379

# LMS Site Configuration
SITE_NAME=lms.localhost
ADMIN_PASSWORD=admin
DEVELOPER_MODE=1

# Application Ports
APP_PORT=8000
SOCKETIO_PORT=9000

# Database Notes:
# - Frappe automatically creates database name based on site name
# - Site: lms.localhost -> DB: _f36731db696e2704 (auto-generated hash)
# - To use custom site name, change SITE_NAME above
# - Examples: mylearning.local, elearning.dev, etc.

# Optional: Email Configuration (for production)
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USE_TLS=1
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-password

# Optional: Custom Domain (for production)
# SITE_DOMAIN=yourdomain.com

# Optional: SSL Configuration (for production)
# SSL_ENABLED=0
# SSL_CERT_PATH=/path/to/cert.pemt.pem
#